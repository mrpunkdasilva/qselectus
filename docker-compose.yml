version: '3.8'

services:
  qselectus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qselectus
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm test